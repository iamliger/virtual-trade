---

# 🤖 로컬 AI 기반 가상 매매 시스템 프로젝트 매뉴얼 (무결점 가이드북)

본 문서는 주식을 전혀 모르는 초보 개발자나, 시간이 지나 다시 프로젝트를 열어본 관리자 모두가 단 5분 만에 환경을 이해하고 복구할 수 있도록 작성된 종합 참고서입니다.

---

## 1. 뼈대 요약 (이 프로젝트는 무엇인가?)
- **목적**: 돈을 쓰지 않고 내 컴퓨터의 로컬 AI(Ollama-Llama3)와 무료 주가 데이터(yfinance)를 엮어 작동하는 가상 주식 매매 프로그램을 만드는 것.
- **내 컴퓨터 주소**: `D:\ProjectWorkspace\AI\virtual-trade`
- **인터넷 백업 주소 (GitHub)**: `https://github.com/iamliger/virtual-trade`
- **내 깃허브 이메일**: `iamliger@nate.com`

---

## 2. 오랜만에 컴퓨터를 켰을 때 꼭 해야 하는 필수 명령어 (Git Bash 터미널)

컴퓨터를 새로 켜거나 VS Code를 다시 열면, 반드시 아래 2개의 명령어를 터미널에 쳐서 **가상환경**을 깨워야 합니다.

```bash
# ① 프로젝트 폴더로 정확하게 이동하기 (윈도우 D드라이브 기준)
cd /d/ProjectWorkspace/AI/virtual-trade

# ② 내 방 전용 파이썬 가상환경(venv) 활성화하기
source venv/Scripts/activate
```
* 💡 **매우 중요 (눈으로 확인하기)**: 위 명령어를 치고 났을 때, 터미널 줄 맨 앞에 **`(venv)`** 라는 글자가 초록색으로 나타나야 준비가 끝난 것입니다. 이게 없으면 프로그램이 작동하지 않고 에러가 납니다.

---

## 3. 🚨 초보자가 반드시 겪는 라이브러리 인식 에러 해결법

### Q. 코드를 잘 짰는데 `yfinance`나 `ollama` 밑에 빨간 줄(또는 노란 줄)이 뜨며 에러라고 나와요!
- **원인**: 컴퓨터에 기본 설치된 엉뚱한 순정 파이썬을 에디터가 바라보고 있어서 그렇습니다. 우리가 설치한 도구들은 프로젝트 폴더 안의 전용 파이썬(`venv`)에 들어있기 때문에 시선을 돌려주어야 합니다.
- **가장 확실한 해결법 (자동화 설정)**:  
  프로젝트 폴더 바로 아래에 `.vscode` 폴더를 만들고 `settings.json` 파일을 생성한 뒤 아래 내용을 넣어두면, 언제 컴퓨터를 켜든 에디터가 알아서 상대경로 가상환경을 찾아가므로 에러가 원천 봉쇄됩니다.
  ```json
  {
      "python.defaultInterpreterPath": ".\\venv\\Scripts\\python.exe"
  }
  ```

---

## 4. 🌐 내 코드를 인터넷(GitHub)에 안전하게 백업하는 방법

개발이 한 단계 끝날 때마다 내 코드를 인터넷 저장소(`origin main`)에 저장하는 공식 3단계 규칙입니다. 

### ① 실전 터미널 명령어 세트
```bash
# 1. 내가 새로 만들거나 수정한 모든 파일을 백업 바구니에 담습니다. (점 '.' 필수)
git add .

# 2. 이번 저장에 어떤 내용을 바꿨는지 한글로 메모(이름표)를 남깁니다.
git commit -m "작업 내용 요약 입력 (예: 메인 엔진 조립 완료)"

# 3. 인터넷 저장소(GitHub)의 메인 공간(main)으로 코드를 쏘아 올립니다.
git push origin main
```

#### Q. git push를 쳤는데 "[rejected] main -> main (fetch first)" 에러가 발생하며 업로드가 안 돼요!
- **해결 방법**:  
  1. 인터넷 파일을 내 컴퓨터로 강제 병합합니다: `git pull origin main --allow-unrelated-histories`
  2. 만약 터미널이 멈춘 것처럼 검은 창이 뜨면 `Esc` -> `:wq` -> `Enter`를 쳐서 빠져나옵니다.
  3. 다시 `git push origin main`을 실행하면 시원하게 업로드됩니다.

---

## 5. 🔑 증권사 서버 연동 핵심 규칙 (한국투자증권 모의투자 정답 공식)

증권사 서버 통신 시 403 에러나 잔고 조회 오류를 방지하기 위해 반드시 지켜야 할 규격입니다.

1.  **서버 주소 (Port 필수)**: `https://openapivts.koreainvestment.com:29443`
2.  **계좌 번호 규격**: `.env`에 `HM_ACCOUNT_NO=4483078002` (10자리) 형태로 저장.
    - 코드상에서 앞 8자리(`CANO`)와 뒤 2자리(`ACNT_PRDT_CD`)로 슬라이싱하여 전송 필수.
3.  **잔고 파싱 문법**: `output2[0].get("dnca_tot_amt", "0")`
    - `output2`가 리스트 배열 구조이므로 반드시 `[0]` 인덱싱을 해야 현금을 읽어올 수 있음.

---

## 6. 🚫 GitHub 백업 시 절대로 올리면 안 되는 파일들 (.gitignore)
- **venv/**: 가상환경 폴더 (용량이 너무 크고 내 컴퓨터 전용임)
- **.env**: 증권사 비밀 키값 (유출 시 보안 위협)
- **account.json**: 개인 가상 자산 잔고 데이터

---

## 7. 📖 프로그램 내부 주식 용어 사전 (사전 툴팁 기능)
초보자가 로그만 보고도 시스템을 이해할 수 있도록 `trade_manager.py`에 구현된 기능입니다.
- **예수금**: 주식을 사기 위해 내 계좌에 넣어둔 '상태 좋은 현금'
- **매수 / 매도**: 주식을 '사고' '파는' 행위
- **평단가**: 주당 평균 매입 원가
- **익절 / 손절**: 이익을 보고 파는 것 / 손해를 보고 탈출하는 것
- **거래세**: 주식을 팔 때 국가에 내는 세금 (단타의 주된 지출 원인)

---

## 8. 🧠 로컬 AI (Ollama - Llama3) 판단 엔진
- **구동**: 유료 API 없이 내 컴퓨터 자원(i7-8700, 64GB RAM)을 활용.
- **제어**: AI가 반드시 JSON 형식으로 응답하게 강제하며, **이유(reason)는 반드시 한국어**로 작성하게끔 시스템 프롬프트를 고정함. (`ai_brain.py` 참고)

- **종합 분석**: 차트 데이터(yfinance)와 실시간 뉴스 헤드라인을 함께 분석합니다.
- **뉴스 필터링**: `yfinance.news`를 통해 수집된 최신 뉴스 3건을 AI에게 전달하여, 단순 가격 변동뿐만 아니라 대외적인 호재/악재를 판단 근거에 포함시킵니다.
- **제어**: AI는 반드시 JSON 형식으로 응답하며, **이유(reason)는 한국어**로 작성하여 어떤 뉴스가 결정에 영향을 주었는지 명시합니다.

---

## 9. 🗓️ 주말 및 공휴일 구동 안내
- **상태**: 주말에는 주식 시장이 닫혀 있으므로 주가가 변하지 않음.
- **활용**: 증권사 모의투자 서버는 열려 있으므로, 루프 엔진이 정상적으로 토큰을 받아오는지와 가상 계좌(`account.json`) 반영 로직을 점검하는 데 최적의 시간임.

---

## 10. 🚀 실전 자동 매매 구동 방법 (월요일 아침용)

### ① 프로그램 실행 전 체크리스트
1. **Ollama 실행**: 바탕화면에서 Ollama를 실행하여 Llama3 모델이 활성화되었는지 확인.
2. **Git Bash**: `cd` 명령어로 이동 후 `source venv/Scripts/activate` 실행 확인.
3. **인터넷**: 실시간 주가 수집을 위해 인터넷 연결 확인.

### ② 실행 명령어
```bash
python main.py
```

### ③ 작동 원리
- **1분 주기**: 프로그램이 1분마다 시장 데이터를 스캔하고 AI에게 조언을 구합니다.
- **실전 수익률**: 단순히 가격 차이가 아니라 **수수료(0.015%)**와 **거래세(0.18%)**를 차감한 실제 주머니에 들어오는 수익을 계산합니다.
- **로그 확인**: AI의 판단 근거와 현재 가상 예수금 현황이 터미널에 실시간으로 중계됩니다.

---